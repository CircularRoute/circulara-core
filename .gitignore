node_modules/
dist/
data/
*.log
.DS_Store
**/registry-data/candidate*
# Local dev secrets (Circulara-generated: CIRCULARA_MASTER_KEY, CIRCULARA_AGENT_TOKEN_SECRET).
# Prod secrets live in Render/Vercel env stores; never commit these. (cto.20260707.003 secrets handling)
.env
.env.local
.env.*.local
# Credentials / private keys - NEVER commit (e.g. the MCP-registry DNS signing key).
*.pem
*.key
